Place the bundled Windows `cloudflared.exe` at `vendor/cloudflared/windows-amd64/cloudflared.exe`.

Use `scripts/prepare_cloudflared_vendor.ps1` to download the official binary into this folder before building release artifacts.
